# Dependency directories
node_modules/
jspm_packages/
web_modules/

# Build outputs
dist/
build/

# ...except the published SDK dist: committed so `github:` installs work even
# when the consumer skips lifecycle scripts (prepare). The SDK's `prepare`
# script still rebuilds it on install when scripts are enabled.
!packages/sdk-react/dist/
!packages/sdk-react/dist/**
.next/
out/
.docusaurus/
.cache/

# IDE and system files
.DS_Store
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.mcp.json

# Databases and docker
postgres_data/
redis_data/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Prisma
/apps/api/prisma/client/
/apps/api/.env
